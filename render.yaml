services:
  - type: web
    name: wikibot
    env: python
    pythonVersion: "3.11.9"
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    envVars:
      - key: GROQ_API_KEY
        sync: false